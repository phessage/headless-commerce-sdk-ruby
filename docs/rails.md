# Rails integration

Configure one client in an initializer using Rails encrypted credentials or environment-backed secrets. Never place confidential credentials in assets. Process future webhooks through Active Job only after signature verification and duplicate-event handling exist in the SDK.
